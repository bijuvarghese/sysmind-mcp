spring:
  config:
    import: "optional:file:.env[.properties],optional:file:../.env[.properties]"
  application:
    name: SysMind MCP

llm:
  url: "${LLM_URL}"
  timeout: "${LLM_TIMEOUT}"
  chat-completions-path: "${LLM_CHAT_COMPLETIONS_PATH}"

news:
  feed-url: "${NEWS_FEED_URL}"
  location-feed-url-template: "${NEWS_LOCATION_FEED_URL_TEMPLATE}"
